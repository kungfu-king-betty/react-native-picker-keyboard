'use strict';

import React from 'react';
import {UnimplementedView} from 'react-native';

class PickerKeyboardIOS extends React.Component {
  static Item = UnimplementedView;
  render() {
    return <UnimplementedView />;
  }
}

export default PickerKeyboardIOS;
